<p>フォロー一覧</p>
<% if @followings.exists? %>
  <% @followings.each do |followings| %>
    <%= link_to followings.email, followings %><br>
  <% end %>
<% else %>
  <p>フォローしているユーザーはいません</p>
<% end %>
<div>
  <%= link_to t('views.translate.back'), :back %>
</div>
